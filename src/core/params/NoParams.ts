// The `NoParams` class is a subclass of `Equatable` that represents a set of parameters with no
// values.
class NoParams {
    // No properties or methods needed as it represents parameters with no values.
  }
  
  export default NoParams;
  